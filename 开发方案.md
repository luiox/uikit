# Rust + Tauri 重构 Poner 启动器开发方案

## 1. 目标与现状评估

### 1.1 项目目标
基于 Rust + Tauri 重写当前 VB6 启动器，优先保证：
1. 在现代 UTF-8 环境稳定运行（中文名称/路径/参数不乱码）；
2. 自动兼容现有 `Data.json`（无需手工迁移）；
3. 尽可能复刻原有使用习惯（分组侧栏、图标列表、编辑窗口、托盘与快捷键行为）；
4. 保持轻量、启动快、内存占用低。

### 1.2 基于现有资料的关键结论
- **重构必要且时机合适**：VB6 在现代系统和编码环境上的维护成本极高，重写可一次性解决编码与可维护性问题。
- **数据兼容可行**：`Data.json` 为标准 JSON，结构稳定（`Name/TargetPath/IconLocation/Arguments/Count`），可直接导入。
- **UI 复刻清晰**：从截图可确认核心交互模型（左侧分类、右侧条目、功能设置页、独立编辑弹窗）。
- **配置兼容有价值**：`Poner.cfg` 可作为“行为偏好”兼容来源（例如当前分组、隐藏行为、快捷键）。

### 1.3 风险评估（按优先级）
| 优先级 | 风险点 | 影响 | 处理策略 |
|---|---|---|---|
| 高 | 旧数据里存在“分组分隔项”（如 `----终端和串口工具----`，且路径为空） | 容易被误判为可执行项 | 在模型中显式标记 `item_type: app/separator` |
| 高 | 路径、参数、中文名、特殊符号兼容（含空格、`[]`、Unicode） | 启动失败或显示异常 | 全链路 UTF-8 + Rust `PathBuf/OsStr` + 统一转义与验证 |
| 中 | 图标提取性能（大量条目） | 首屏卡顿 | 懒加载 + 缓存缩略图 + 后台线程提取 |
| 中 | 全局快捷键冲突 | 功能不可用 | 提供冲突检测、失败回退、可配置热键 |
| 中 | 旧配置字段语义不完整（如 `Title` 编码） | 无法 1:1 还原少量行为 | 采用“可识别即迁移、不可识别则忽略并告警”策略 |
| 低 | 多显示器/缩放下窗口吸附细节 | 体验偏差 | 使用窗口状态插件 + 边缘吸附阈值可配置 |

---

## 2. 技术路线建议

### 2.1 Tauri 版本建议
**建议采用 Tauri 2.x（新项目优先）**，原因：
- 生态和安全模型更现代（权限能力配置更清晰）；
- 长期维护周期更好；
- 与 Rust 新版本协同更稳定。

> 说明：你草稿里的 `allowlist`、部分插件写法属于 Tauri 1.x 风格。若追求长期维护，建议直接按 2.x 重构，不建议新项目继续压在 1.x。

### 2.2 前后端职责
- **前端（WebView，原生 HTML/CSS/JS）**
  - 列表展示、分类切换、搜索过滤、右键/按钮操作、编辑窗口表单。
- **Rust 后端**
  - 数据读写、迁移转换、程序启动、图标提取、托盘、全局快捷键、窗口管理。
- **IPC 边界**
  - 前端只传递“意图”；后端负责校验和执行，避免前端直接拼接命令。

### 2.3 依赖建议（精简版）
- 必选：`tauri`、`serde`、`serde_json`、`tokio`、`anyhow`、`thiserror`、`dirs`
- 建议：`tracing`、`tracing-subscriber`、`once_cell`
- 可选：`tauri-plugin-single-instance`、`tauri-plugin-global-shortcut`、`tauri-plugin-window-state`

> 不建议默认启用“全量 API”；按最小权限启用能力，减少安全面与维护成本。

---

## 3. 兼容迁移设计（核心）

### 3.1 旧数据结构（已确认）
顶层对象是“分组名 -> 启动项数组”，单项字段：
- `Name`
- `TargetPath`
- `IconLocation`
- `Arguments`
- `Count`

### 3.2 新版内部模型（建议）
```text
LauncherData
  version: 2
  groups: [Group]

Group
  id: string
  name: string
  order: i32
  items: [LaunchItem]

LaunchItem
  id: string
  item_type: "app" | "separator"
  name: string
  target_path: string
  icon_path: string
  arguments: string
  launch_count: u64
  last_launch_at: Option<timestamp>
  enabled: bool
```

### 3.3 自动兼容规则
1. 首次启动检测：
   - 若发现同目录 `Data.json`，进入“兼容导入模式”；
   - 导入成功后生成新格式数据文件（如 `launcher.v2.json`），并保留原文件不改写。
2. 分隔项识别：
   - 当 `TargetPath` 与 `IconLocation` 均为空，或名称形如 `----xxx----`，标记为 `separator`。
3. 路径与参数处理：
   - 保持原值，不做破坏性格式化；
   - 启动前做存在性检查与可执行性判断；
   - 若不可执行，UI 仅提示，不阻塞其他项。
4. 编码策略：
   - 全部按 UTF-8 读写；
   - 旧文本若出现非 UTF-8 异常字节，使用“容错解码 + 告警日志 + 原始备份”。
5. `Count` 兼容：
   - 导入为 `launch_count`；运行时原子更新；支持排序“按使用频率”。

### 3.4 Poner.cfg 兼容策略（可选但推荐）
可迁移字段（能明确解释的）：
- `CurrentTab` / `ActivatedTab` -> 启动时默认选中分组索引
- `ExecuteHide` -> 启动后是否自动隐藏主面板
- `FullScreenHide` -> 全屏时隐藏策略
- `DisableAutoUpdate*` -> 新项目可忽略（若无在线更新）

不确定字段（保守处理）：
- `Title`（疑似编码/加密串）
- `Handle`（运行态句柄）
- `Size`（坐标语义需结合旧程序逻辑验证）

策略：**可识别就迁移；不确定就跳过并记录日志**。

---

## 4. 功能范围（按版本）

> 补充定位：产品目标从“启动器”升级为“AI 开发工作台底座”，包含启动器、全局库管理（Skills/MCP/Prompt）与项目级启用配置。

### 4.1 MVP（第一阶段必须完成）
1. 读取并展示所有分组与启动项（含 separator）；
2. 单击启动程序，正确传参；
3. 编辑窗口（非模态）支持新增/修改/删除；
4. 自动保存到新格式文件；
5. 托盘驻留 + 显示/隐藏主窗口；
6. 全局快捷键呼出主窗口（默认 Alt+1，可改）；
7. UTF-8 全链路稳定（中文不乱码）。

### 4.2 增强版（第二阶段）
1. 图标懒加载与缓存；
2. 窗口吸附边缘；
3. 搜索/过滤；
4. 排序（按名称/使用次数/最近使用）；
5. 导入导出（旧版 JSON / 新版 JSON）；
6. Skills 全局库管理（导入、安装到统一目录、复制到项目）。

### 4.3 第三阶段（可选）
1. MCP 全局管理（注册、启动/停止、日志、健康检查）；
2. Prompt 片段库管理（分类、变量模板、一键复制）；
3. 项目级配置（项目启用哪些 MCP/Skills/Prompt）；
4. 自动备份与回滚；
5. 插件化动作（打开目录、打开网址、执行脚本）。

---

## 5. 全局库与项目级配置设计（新增）

### 5.1 设计目标
1. **全局库统一管理**：Skills、MCP、Prompt 都有统一仓库目录和元数据，不再手工散落维护；
2. **项目按需启用**：每个项目声明自己的启用集，支持不同项目使用不同能力组合；
3. **低摩擦切换**：切换项目时自动完成“启用能力 + 启动必要 MCP + 注入常用 Prompt”。

### 5.2 统一概念模型
```text
GlobalWorkspace
  skills_repo: GlobalSkillsRepo
  mcp_repo: GlobalMcpRepo
  prompt_repo: GlobalPromptRepo
  project_profiles: [ProjectProfile]

ProjectProfile
  project_id: string
  project_path: string
  enabled_skills: [skill_id]
  enabled_mcp_servers: [mcp_server_id]
  enabled_prompt_sets: [prompt_set_id]
  env_overrides: map<string, string>
  startup_actions: [action]
```

### 5.3 Skills 全局库方案
- 全局目录建议：`%APPDATA%/NAssistant/skills/`（Windows），每个 skill 一个目录；
- 每个 skill 附带 `skill.manifest.json`（`id/name/version/source/hash/tags`）；
- 支持两种启用模式：
  - **复制模式**：复制到项目目录，兼容性最好；
  - **链接模式**：软链接/硬链接减少重复，占空间更小（Windows 需权限判断）。
- 项目启用时执行冲突检测：同名不同版本、缺少依赖、路径不可达。

### 5.4 MCP 全局库方案
- 每个 MCP 服务器有注册信息：`id/name/transport/http_port/command/args/cwd/env`；
- 内置生命周期管理：启动、停止、重启、查看日志、健康检查、端口冲突提示；
- 对外暴露统一连接信息（HTTP 或可兼容的桥接层），让 VS Code 等客户端按项目自动接入；
- 提供“按项目启动集”：打开项目时仅启动该项目声明的 MCP 服务。

### 5.5 Prompt 片段库方案
- Prompt 采用“片段 + 模板变量”模式：
  - 片段可按 `场景/语言/任务` 分类；
  - 支持变量占位符（如 `${module_name}`）；
  - 支持组合片段（System + Task + Constraints）。
- 全局库管理能力：导入、去重、版本标记、收藏、最近使用；
- 项目级扩展：项目可覆盖或新增本地 Prompt 集，不污染全局库。

### 5.6 项目级配置文件（建议）
建议在项目根目录使用 `na.project.json`：
```json
{
  "version": 1,
  "projectId": "demo-embedded-fw",
  "enabledSkills": ["embedded-debug", "c-rust-review"],
  "enabledMcpServers": ["mcp-files", "mcp-git", "mcp-docs"],
  "enabledPromptSets": ["firmware-default", "code-review-cn"],
  "env": {
    "RUST_LOG": "info"
  },
  "startupActions": [
    "start-mcp",
    "open-launcher-panel"
  ]
}
```

### 5.7 配置优先级（避免混乱）
1. 项目本地配置（`na.project.json`）
2. 用户全局配置（`%APPDATA%/NAssistant/config.json`）
3. 程序默认配置

同名键按“就近覆盖”原则处理，且在 UI 中明确显示最终生效值。

### 5.8 一键工作流（关键体验）
1. 用户选择项目；
2. 程序加载项目 profile；
3. 校验并启用对应 skills；
4. 启动所需 MCP 服务器并完成健康检查；
5. 挂载项目常用 Prompt 集；
6. 进入主界面，用户可立即开始开发。

---

## 6. 目录与模块设计

```text
launcher/
  src-tauri/
    src/
      main.rs
      app/
        commands.rs        # IPC 命令
        window.rs          # 主窗/编辑窗管理
        tray.rs            # 托盘
        hotkey.rs          # 全局快捷键
        mcp_runtime.rs     # MCP 生命周期管理
      domain/
        model.rs           # Group/LaunchItem
        profile.rs         # ProjectProfile
        skill.rs           # Skill 元数据模型
        prompt.rs          # Prompt 元数据模型
        mcp.rs             # MCP 元数据模型
        validator.rs       # 数据校验
      infra/
        storage.rs         # JSON 读写
        migrate.rs         # 旧版数据迁移
        icon.rs            # 图标提取与缓存
        process.rs         # 进程启动
        cfg_compat.rs      # Poner.cfg 兼容解析
        skill_repo.rs      # Skills 全局仓库管理
        prompt_repo.rs     # Prompt 全局仓库管理
        mcp_repo.rs        # MCP 注册仓库管理
        profile_store.rs   # 项目配置读写
        health_check.rs    # MCP 健康检查
  ui/
    index.html
    editor.html
    styles.css
    app.js
    editor.js
    skills.html
    mcp.html
    prompts.html
    project_profile.html
```

---

## 7. 关键实现细节

### 7.1 启动执行策略（Windows）
- 优先使用 Rust `std::process::Command` 直接执行；
- 参数按数组传入，不走 shell 拼接，避免空格/转义问题；
- 对 `.exe/.bat/.cmd/.lnk` 分类型处理；
- 执行失败返回结构化错误（错误码 + 用户可读消息）。

### 7.2 编辑窗口（非模态）
- 使用独立窗口 label（如 `editor`）；
- 单例编辑窗：已存在则聚焦，不重复创建；
- 主窗口与编辑窗口通过事件总线同步数据变更。

### 7.3 性能策略
- 启动时仅加载文本数据，不阻塞提取图标；
- 图标缓存目录按文件哈希命名；
- UI 分批渲染（例如每批 30 条），避免首屏卡顿。

### 7.4 稳定性与可恢复
- 写入采用“临时文件 + 原子替换”；
- 每次保存前自动保留最近 N 份备份；
- 崩溃恢复时优先读取最近完整文件。

### 7.5 Skills 启用策略
- 统一做 `manifest` 校验（ID、版本、哈希）再启用；
- 复制/链接策略可配置，默认复制保证兼容；
- 项目切换时自动计算“增量变更”（仅新增/移除变化项）。

### 7.6 MCP 运行策略
- 每个服务独立进程管理，保留 PID、启动时间、端口、状态；
- 提供“失败重试（有限次）+ 指数退避”；
- 支持日志滚动文件，便于排障和审计。

### 7.7 Prompt 片段策略
- 片段存储为结构化 JSON（含标签、变量定义、版本）；
- 变量替换有预览模式，避免误提交；
- 项目 Prompt 与全局 Prompt 分层展示，可一键合并视图。

---

## 8. 开发里程碑与工期建议

| 里程碑 | 时长（工作日） | 交付物 |
|---|---:|---|
| M1：脚手架与基础窗口 | 2-3 | 主窗口、托盘、快捷键、基础 IPC |
| M2：数据层与兼容导入 | 3-4 | `Data.json` 自动导入、新模型持久化、兼容日志 |
| M3：核心交互与编辑窗 | 4-5 | 列表/分组/编辑/删除/保存全流程 |
| M4：启动执行与错误处理 | 2-3 | 程序启动、参数处理、错误提示 |
| M5：性能与体验打磨 | 3-4 | 图标缓存、窗口吸附、基础搜索 |
| M6：测试与打包发布 | 2-3 | 回归清单、安装包、迁移说明 |
| M7：Skills 全局库 | 3-5 | Skills 仓库、manifest、项目启用 |
| M8：MCP 管理器 | 4-6 | MCP 注册、生命周期、日志与健康检查 |
| M9：Prompt 与项目配置 | 3-5 | Prompt 库、`na.project.json`、一键切换工作流 |

**总计建议：26-38 个工作日（1 人）**。

---

## 9. 测试与验收标准

### 9.1 兼容验收
- 使用你现有 `Data.json` 直接导入后：
  - 分组数量一致；
  - 条目数量一致；
  - separator 显示正确；
  - 中文名称无乱码。

### 9.2 功能验收
- 任意 20 个不同类型路径可成功启动（含带参数项）；
- 编辑新增/修改/删除后重启仍保留；
- 快捷键可呼出/隐藏主窗口；
- 托盘“显示/退出”可用。

### 9.3 性能验收（建议目标）
- 冷启动 < 1.5s（常规桌面硬件）；
- 内存常驻 < 120MB（首版）；
- 1000+ 条目列表滚动无明显卡顿。

### 9.4 Skills/MCP/Prompt 验收
- Skills：同一 skill 在不同项目启用策略正确生效，无脏覆盖；
- MCP：按项目启动集可一键拉起，异常服务可检测并提示；
- Prompt：全局与项目片段可区分管理，变量替换结果正确；
- 项目切换：切换后生效集与 UI 展示一致。

---

## 10. 与你现有草稿相比的优化建议

1. **版本策略升级**：新项目优先 Tauri 2.x，不建议继续 1.x；
2. **兼容策略前置**：将 `Data.json/Poner.cfg` 迁移定义为 P0（而不是后补）；
3. **数据模型扩展**：新增 `item_type` 与 `id`，避免 separator 与普通项混淆；
4. **安全执行策略**：尽量不用 shell 字符串拼接，改为参数化执行；
5. **可恢复写入**：采用原子写 + 备份，提高稳定性。
6. **能力域扩展**：将 Skills/MCP/Prompt 作为核心能力域，而非附加功能；
7. **项目配置中台化**：引入 `na.project.json`，让项目能力可复现、可迁移。

---

## 11. 下一步执行建议

1. 先完成最小可运行骨架（主窗 + 托盘 + 快捷键 + 数据加载）；
2. 接入 `Data.json` 兼容导入并对你的真实数据做首轮验证；
3. 实现非模态编辑窗与保存逻辑；
4. 新增全局库底层（Skills/MCP/Prompt 仓库 + 元数据）；
5. 定义并接入 `na.project.json`，完成项目级启用链路；
6. 最后做图标缓存、窗口吸附、日志可视化等体验增强。

如果你确认这版方案，我下一步可以直接帮你生成项目初始骨架（Tauri 2.x + 原生前端 + 兼容导入模块），并继续补上 `Skills/MCP/Prompt + 项目配置` 的数据结构与接口骨架。