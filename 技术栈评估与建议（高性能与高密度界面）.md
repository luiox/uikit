# 启动器技术栈评估与建议（高性能 + 高密度界面）

> 日期：2026-02-20  
> 适用项目：nassistant（当前：Rust + Tauri v2 + Vue 3）

---

## 1. 背景与目标

你当前的核心诉求是：
1. **启动速度快**（冷启动、热启动都快）；
2. **资源占用小**（常驻内存低、空闲 CPU 低）；
3. **响应快**（列表滚动、搜索、打开编辑、拖拽导入、启动动作即时）；
4. **高密度界面**（信息密度高，不要典型 Web 的留白风格）；
5. **可深度自定义外观**（Windows 风格一致且可控）。

当前项目功能已经跑通，问题集中在：
- 多窗口 + 自定义无边框效果下的视觉问题（残影、虚框、焦点体验差）；
- 对“Web UI 天然风格”的不满意（密度与系统感）；
- 对性能天花板与稳定性的担忧。

---

## 2. 结论先行

**不建议立刻全量重写到 Win32/duilib。**

建议采用“两阶段决策”：
- **阶段 A（2~3 周）**：在现有 Tauri 架构上做一次“性能 + UI 架构止损”改造，快速验证是否能达到目标；
- **阶段 B（触发条件满足时）**：若仍达不到目标，再迁移到“Rust 后端 + 原生 UI（优先 Slint，其次 Win32/C++）”。

这样做的收益是：
- 避免在功能已成型时立即付出高迁移成本；
- 用真实数据判断是否必须换栈，而不是凭体感直接重构；
- 保留你已写好的 Rust 业务逻辑与数据兼容能力。

---

## 3. 候选技术栈对比（面向你的目标）

评分规则：1~5（5 最优）

| 方案 | 启动/内存 | 响应与稳定 | 高密度 UI | 自定义能力 | 开发效率 | 迁移成本 | 综合结论 |
|---|---:|---:|---:|---:|---:|---:|---|
| 继续 Tauri + Vue（优化后） | 3 | 3 | 3 | 3 | 5 | 1 | **先做止损最划算** |
| Tauri + 非 Web 前端（不可行常规路径） | 2 | 2 | 2 | 2 | 2 | 4 | 不推荐 |
| Rust + Slint | 4 | 4 | 4 | 4 | 3 | 3 | **最平衡的中长期方案** |
| Rust + egui | 4 | 4 | 3 | 3 | 4 | 3 | 工具感强，桌面质感一般 |
| C++ Win32/duilib | 5 | 5 | 5 | 5 | 2 | 5 | 性能顶，但成本/维护最高 |
| C# WinUI 3/WPF | 3~4 | 3~4 | 4 | 4 | 4 | 4 | Windows 生态好，原生感强 |

### 关键解释
- 你是 **Windows-only 启动器**，Win32/duilib 的确能做到极致性能与自绘，但重构成本和维护复杂度也最高。
- 对你当前项目来说，**Rust + Slint** 是更现实的“高性能 + 高密度 + 自定义”升级路径：
  - 性能和资源占用明显优于 Web UI；
  - 保留 Rust 业务层，迁移主要在 UI 层；
  - 比纯 Win32/duilib 更可维护。

---

## 4. 现有 Tauri 架构的主要瓶颈（可修）

1. **独立编辑窗口（第二个 WebView）**：
   - 在 Windows 无边框 + 阴影 + 透明效果下，容易出现重绘和焦点问题。
2. **Web 风格默认间距偏大**：
   - 信息密度不是技术限制，而是样式体系与交互规则未按“高密度桌面工具”调优。
3. **UI 线程任务碎片化**：
   - 频繁刷新、窗口事件、图标提取若不分层，会影响体感响应。

这些问题都可以先通过架构调整显著改善，不一定要立刻换栈。

---

## 5. 推荐路线（可执行）

## 5.1 阶段 A：先做“止损优化”（2~3 周）

### A1. 窗口策略
- 取消编辑子窗口，改为主窗口内模态层（单 WebView）；
- 编辑区域使用固定尺寸面板，不做透明/复杂阴影；
- 尽量使用系统装饰或最小化自定义窗口效果。

### A2. 高密度界面改造
- 全局字体 12~13px，行高紧凑化；
- 列表项高度压缩到 26~30px；
- 减少留白，增强快捷键操作比例（减少鼠标路径）；
- 图标与文本布局按“工具软件”风格，而非内容网站风格。

### A3. 性能措施
- 图标懒加载 + LRU 缓存 + 后台提取；
- 列表渲染最小更新（避免全量 refresh）；
- 把重操作放到 Rust 异步任务，前端只接收结果；
- 增加性能埋点（启动耗时、首屏耗时、拖拽处理耗时、内存峰值）。

### A4. 验收阈值（建议）
- 冷启动可见首屏：**< 500ms（目标） / < 800ms（可接受）**；
- 常驻内存：**< 120MB（目标） / < 180MB（可接受）**；
- 常规操作响应：**< 50ms**；
- 拖拽创建启动项（单条）：**< 100ms**。

> 若阶段 A 达标：继续 Tauri，收益最大。

## 5.2 阶段 B：未达标再迁移

若阶段 A 连续两轮优化仍明显不达标，则迁移：

### 优先方案：Rust + Slint
- 保留现有 Rust 业务模块（数据、启动、图标、配置兼容）；
- 重写 UI 层与窗口交互；
- 目标：获得原生感、更低开销、可控的高密度布局。

### 备选方案：C++ Win32/duilib
- 仅在你明确追求“极致性能 + 深度自绘 + 可接受高维护成本”时采用；
- 需预留更长工期和更严格工程规范（崩溃诊断、内存安全、UI 组件库长期维护）。

---

## 6. 是否改技术栈：决策规则

满足以下任一条，建议进入迁移立项：
1. 阶段 A 后冷启动仍 > 800ms，且无法继续优化；
2. 常驻内存长期 > 180MB，且影响常驻体验；
3. 窗口/重绘问题仍频发，影响可用性；
4. 你对“Web 视觉质感”不可接受，且样式重构后仍不满意。

若以上都不满足，不建议改栈，继续在 Tauri 演进。

---

## 7. 成本与风险评估（粗估）

| 路线 | 预计周期（单人） | 风险 | 备注 |
|---|---:|---|---|
| Tauri 止损优化 | 2~3 周 | 低 | 最快看到收益 |
| 迁移到 Slint | 4~8 周 | 中 | UI 重写，但后端可复用 |
| 迁移到 Win32/duilib | 8~16 周 | 高 | 性能最好，但维护成本高 |

---

## 8. 对你当前项目的建议（最终）

**建议顺序：**
1. 立即执行阶段 A（先把编辑窗并回主窗，做高密度与性能埋点）；
2. 两周后按数据决策，而不是凭体感；
3. 若未达标，优先迁移到 **Rust + Slint**；
4. 仅在你明确接受高复杂度时，才考虑 Win32/duilib。

一句话总结：
**现在不该“立刻重写”，该先“低成本验证上限”；真要换栈，优先 Slint，不要直接跳 Win32 除非你要极致且能承担维护代价。**

---

## 9. 附：下一步执行清单（可直接开工）

1. 删除编辑子窗口，改主窗内模态编辑；
2. 建立性能埋点：启动、首屏、拖拽导入、启动动作；
3. 做一版高密度主题（仅改 spacing/字体/列表高度）；
4. 跑一轮 500~1000 条目压测；
5. 输出“达标/不达标”报告，再决定是否立项迁移。
