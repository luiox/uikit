# cpp-duilib-launcher 后续计划列表

## P-1：数据安全层（最高优先级，先于一切功能）

> 设计原则：可回滚的操作不设防，不可回滚的操作设大防。
> 安全性来自可回滚性，不来自输入咒语。

### 1. 备份体系（基于现有 WriteTextAtomic）

- [ ] 每次保存前将旧 `launcher.v2.json` 轮转入 `backups/`，保留最近 5 份
- [ ] 每日首次保存额外存日期快照 `launcher.v2.%Y%m%d.json`，保留 30 天
- [ ] Load 时若检测到损坏，自动提示从最近备份恢复（Load 兜底已有，补"选备份"UI）

### 2. 删除分级防护

- [ ] 单条删除 → 软删除：移入内置「回收站」分组 + Toast「已删除 · Ctrl+Z 撤销」，零摩擦
- [ ] 回收站内删除 → 真删除，但可从任意备份找回（备份即最后防线）
- [ ] 清空分组 / 批量删除 → 确认框要求输入**将被删除的条目数量**（强迫看清后果，比 MD5 可读）
- [ ] 导入覆盖 / 重置全部 → 输入当前数据文件 MD5 前 6 位（核弹级仪式只留给核弹级操作）

### 3. 编辑流程两段式

- [ ] 重命名/编辑统一走对话框：入口点击 → 对话框修改 → 明确「确定」提交
- [ ] Esc 与失焦一律视为放弃，绝不静默落盘

### 4. 操作日志（追加型 journal）

- [ ] 所有增删改移动追加一行到 `operations.log`（时间戳 + 动作 + 数据摘要）
- [ ] 用途：误操作可追溯；也为将来"时间机器"式回放留接口

---

## P0-M：Poner 数据迁移器（打通日常使用的最后一步）

> 数据源实测：`D:\desktop\poner\Data.json`（44KB，12 组，字段与 v2 全同构）。
> 映射表：Name/TargetPath/IconLocation/Arguments/Count 一一直拷；
> 组名→Group.name+order；item_type/enabled/id 填默认。

- [ ] `ImportPonerData(path)`：解析 → 按映射生成 LauncherData → 原子落盘
- [ ] **幂等可重复导入**：以 (组名, TargetPath) 为键——已存在则更新 Count/名称，新条目追加
       （原因：迁移期 Poner 仍在日用，数据持续变化，需反复导入直到切换日）
- [ ] 首次启动检测 `legacy_root\Data.json` 存在 → 弹「一键导入」提示
- [ ] 导入动作本身先走一遍安全层：导入前自动快照当前数据

---

## P0：优先补齐用户最容易直接碰到的缺口

### 1. 搜索命令解析

目标：让搜索框不仅能过滤，还能执行 VB6 风格命令。

建议拆分：

1. 在 `SearchController` 增加命令识别层
2. 支持精确命令：
   - `cmd`
   - `setting`
   - `shutdown`
   - `reboot`
   - `logoff`
   - `screenoff`
3. 支持“百度 + 关键词”模式
4. 保持原有名称过滤不回退

### 2. 项目编辑窗 MVP

目标：替代当前“重新选 exe”的简化编辑流程。

最小版本字段：

- 标题
- 路径
- 参数
- 图标位置

优先建议：

- 先用简化 DuiLib 自绘窗完成 MVP
- 先不追求完全复刻 VB6 布局，只保证字段完整

### 3. 设置窗 MVP

目标：先把关键配置收口到一个统一入口。

建议首批字段：

- 热键
- 启动后隐藏/执行后隐藏
- 默认窗口宽高
- 分组栏宽度

### 4. 主菜单补全

当前菜单按钮已存在，适合继续补执行逻辑。

建议顺序：

1. `Sort By Name`
2. `Import Data`
3. `Export Data`
4. `Settings`
5. 再补 VB6 的特殊“新建项目”子项

---

## P1：把界面风格进一步收敛到 VB6

### 1. 分组区背景改白

- 将 `group_panel` 从 `E6E6E6` 调整为 `FFFFFF`
- 保留分隔线和 hover 的浅灰层次

### 2. 全量文案中文化

优先中文化：

- 顶部菜单
- 分组右键菜单
- 项目右键菜单
- 状态栏提示
- 对话框标题与按钮文字

### 3. 搜索区视觉继续收敛

- 当前容器背景已对齐 `210`
- 下一步可微调输入框与边框，让它更接近 VB6 的灰底容器 + 白输入区感受

---

## P2：高还原度增强项

### 1. 独立阴影窗 / 辅助窗

- 复刻 `frmShadow` 的拖动与阴影行为
- 可在视觉完全稳定后再做

### 2. 完整资源管理器右键菜单

- 由“属性页近似实现”升级为真正的 Explorer context menu

### 3. 更细的像素级还原

- 顶栏高度
- 列表行高
- 留白
- hover/pressed 灰阶变化

---

## 推荐开发顺序

0. **数据安全层 P-1 全部**（备份/软删除/两段式/journal）
1. 搜索命令解析
2. 项目编辑窗 MVP
3. 设置窗 MVP
4. 主菜单补齐
5. 中文文案统一
6. 白底分组区与视觉收敛
7. Explorer 真右键菜单
8. 阴影窗与像素级还原

> 优先级说明：可用性缺口（P-1/P0）优先于 VB6 还原度（P1/P2）。
> 还原度是情怀分，安全层和日用摩擦是生死分。

---

## 每阶段建议验证

### 功能验证

- `xmake build -r nassistant-duilib`
- 手工运行并验证菜单/搜索/编辑流程

### 回归验证

- `xmake run backend_tests`

### 文案与视觉验证

- 对照 `VB6_UI参数与需求分析.md`
- 对照 `C++实现_vs_VB6标准_逐项差异表.md`
- 逐项更新“已实现 / 未实现”文档
