# cpp-duilib-launcher 未实现功能清单

> 本文只列“代码核对后仍缺失或仅部分实现”的项。

## 一、主菜单仍未完整对齐 VB6

当前顶部菜单按钮已经存在，但菜单内容和执行逻辑仍未完全补齐。

### 已缺失或未落地的部分

- 新建项目菜单仅有 `Custom`
  - 缺：空项目、计算机、控制面板、回收站、注销、关机、重启等 VB6 语义入口
- 排序仅有占位提示
  - `Sort By Name` 当前返回 `not implemented yet`
- 锁定自动隐藏：未实现
- 路径转换：未实现
- 导入/导出：菜单项已存在，但逻辑未实现
- 设置：菜单项已存在，但逻辑未实现

## 二、搜索命令语义未实现

`SearchController` 当前只负责：

- 开关搜索模式
- 文本变化时重渲染列表

仍未实现 VB6 提到的命令解析：

- `cmd`
- 百度搜索
- `setting`
- `shutdown`
- `reboot`
- `logoff`
- `screenoff`

## 三、项目编辑体验仍是简化版

当前“新增/编辑项目”本质上仍然是：

- 弹系统文件选择框
- 重新选择一个可执行文件

尚未实现 VB6 版编辑窗的完整字段：

- 标题
- 路径
- 参数
- 图标位置
- 图标预览

## 四、设置窗口缺失

目前还没有单独的设置窗口。`DialogManager` 只管理分组弹层。

缺失能力包括：

- 热键设置
- 执行后隐藏
- 窗口尺寸/布局相关选项
- 多面板设置页结构

## 五、视觉细节仍未完全收敛到 VB6

### 1. 分组区背景

- 目前分组区仍是 `0xFFE6E6E6`
- VB6 基线更偏向白底分组/白底内容区

### 2. 顶栏/状态/菜单文案

- 当前大量文案仍为英文：
  - `Add Group`
  - `Rename Group`
  - `Delete Group`
  - `Run as administrator`
  - `Open file location`
  - `Settings`
  - `Exit`

与原版“中文为主”的风格仍不一致。

### 3. 阴影/辅助窗

- VB6 有专门的 `frmShadow`
- 当前尚无独立阴影窗或等效拖动辅助窗

## 六、资源管理器菜单仍是近似实现

当前 `ShowSelectedItemShellMenu()` 实现的是：

- 直接调用 `ShellExecute(..., "properties", ...)`

这更接近“打开属性”，并不等于完整的资源管理器右键上下文菜单。

## 七、部分菜单项虽然存在，但仍是占位态

这些入口用户已经能点到，但功能尚未真正完成：

- Sort By Name
- Import Data
- Export Data
- Settings

这类项优先级很高，因为它们已经暴露在 UI 上，用户会自然认为可用。

## 八、结论

当前最关键的缺口不在基础 CRUD，而在“高级操作与完整产品化体验”：

1. 搜索命令语义
2. 完整编辑窗
3. 设置窗
4. 主菜单补齐
5. 视觉与文案收敛
