# 当前 C++ 实现 vs VB6 标准：逐项差异表

> 基线文档：`VB6_UI参数与需求分析.md`
>
> 对比范围：`cpp-duilib-launcher/src/app_window.cpp`、`cpp-duilib-launcher/src/main.cpp` 与 `dec/*.frm/*.ctl` 提炼标准。

## 判定说明
- 对齐：当前实现满足 VB6 标准或等价实现。
- 部分对齐：核心行为存在，但细节缺失或参数偏差。
- 未对齐：当前缺失该能力或方向明显不同。

## 逐项差异

| 模块 | VB6 标准 | 当前 C++ 实现 | 差异判定 | 优先级 | 建议动作 |
|---|---|---|---|---|---|
| 主窗标题 | 标题为 Poner | `main.cpp` 与顶部标题均为 Poner | 对齐 | P2 | 保持 |
| 主窗边框形态 | 无系统边框（BorderStyle=None） | `OnCreate` 去除 `WS_CAPTION` | 对齐 | P2 | 保持 |
| 顶栏背景 | `RGB(230,230,230)` | 顶栏 `0xFFE6E6E6` | 对齐 | P2 | 保持 |
| 主文本色 | `RGB(90,90,90)` | 主文本 `0xFF5A5A5A` | 对齐 | P2 | 保持 |
| 顶栏按钮集合 | 搜索/菜单/退出三入口 | 当前仅搜索+关闭 | 未对齐 | P0 | 增加“菜单按钮”并挂主菜单 |
| 主菜单（顶部） | 新建、排序、锁自动隐藏、路径转换、导入导出、更新、设置、官网、退出 | 当前无顶部主菜单 | 未对齐 | P0 | 实现顶部菜单按钮与完整菜单项 |
| 分组区背景 | Tabs/Groups 以白底为主（255） | 当前分组面板与列表为 `E6E6E6` | 部分对齐 | P1 | 改为白底或做运行态核对后再定稿 |
| 分组行左边距 | 原版更贴边 | 已去除左 padding（0） | 对齐 | P2 | 保持 |
| 分组右键菜单 | 新建/重命名/删除 | 当前新建/重命名，缺删除 | 未对齐 | P0 | 增加删除分组并处理“组内项目迁移” |
| 分组重命名交互 | Enter 确认，Esc 取消 | 弹层内 Enter/Esc 已支持 | 对齐 | P1 | 保持 |
| 项目右键菜单-基础 | 编辑/删除/移动到分组 | 当前已支持 | 对齐 | P1 | 保持 |
| 项目右键菜单-扩展 | 管理员运行/打开文件位置/资源管理器菜单/复制完整路径 | 当前缺失 | 未对齐 | P0 | 新增 4 个命令与执行逻辑 |
| 搜索入口样式 | 搜索区 BG=210, FG=90 | 当前搜索输入白底+灰边 | 部分对齐 | P1 | 增加搜索容器背景 210（输入框可保持白） |
| 搜索行为-过滤 | 输入实时刷新结果 | 已实现 `TEXTCHANGED -> RenderItems` | 对齐 | P1 | 保持 |
| 搜索行为-命令 | 支持 cmd/百度/setting/shutdown/reboot/logoff/screenoff | 当前仅名称过滤与回车启动 | 未对齐 | P0 | 增加命令解析分支 |
| 项目新增/编辑体验 | VB6 编辑窗支持标题/路径/图标/参数 | 当前新增/编辑依赖文件选择框，字段简化 | 未对齐 | P0 | 补齐“编辑窗”能力（至少标题/路径/参数/图标） |
| 设置窗口 | VB6 独立设置窗（多面板） | 当前未实现 | 未对齐 | P0 | 新建设置窗口 MVP（先做关键项） |
| 阴影/拖动辅助窗 | VB6 有 `frmShadow` | 当前无独立阴影窗 | 未对齐 | P2 | 作为后续视觉增强项 |
| 顶栏关闭行为 | 退出语义（菜单含 Exit） | 当前 close 按钮触发 `WM_CLOSE` 退出 | 对齐 | P1 | 保持 |
| 快捷键 | Enter 启动，Delete 删除，Esc 退出搜索 | 当前已实现 | 对齐 | P1 | 保持 |
| 拖拽导入 | 支持外部数据导入/项目创建 | 当前支持 `WM_DROPFILES` 批量导入 | 对齐 | P1 | 保持 |
| 文案语言 | 原版菜单与文案中文为主 | 当前多数英文文案 | 部分对齐 | P2 | 按原版文案逐步本地化 |

---

## 建议落地顺序（按收益）

1. P0：补齐主菜单 + 分组删除 + 项目右键扩展命令 + 搜索命令解析 + 编辑窗/设置窗最小可用版。
2. P1：分组背景与搜索区视觉收敛；统一文案与状态提示风格。
3. P2：阴影窗与动画/微交互等像素级还原。

---

## 可直接转任务的清单（开发项）

- 新增顶部菜单按钮与 `MainMenu` 全量条目。
- 分组菜单新增“删除分组”，并实现“项目迁移到首组”策略。
- 项目菜单新增：管理员运行、打开文件位置、资源管理器菜单、复制完整路径。
- 搜索模式新增命令解析（cmd、百度、setting、shutdown/reboot/logoff/screenoff）。
- 新建“项目编辑窗”以替代仅选文件流程。
- 新建“设置窗 MVP”承载核心开关配置。
- 白底分组区 + 搜索容器 210 灰底微调。
