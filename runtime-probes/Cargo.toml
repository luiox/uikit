[package]
name = "runtime-probes"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "probe-rust-idle"
path = "src/probe_rust_idle.rs"

[[bin]]
name = "probe-win32-idle"
path = "src/probe_win32_idle.rs"

[dependencies]

[target.'cfg(windows)'.dependencies]
windows-sys = { version = "0.59", features = [
  "Win32_Foundation",
  "Win32_Graphics_Gdi",
  "Win32_System_LibraryLoader",
  "Win32_UI_WindowsAndMessaging"
] }

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
panic = "abort"
